# Project Mandates

## Git Workflow
- **Commit Messages:** Always provide descriptive commit messages that explain the "why" and "what" of the changes.
- **Syncing:** Whenever a task is completed, ensure changes are committed and pushed to the remote repository (if one is configured).
- **Security:** Never stage or commit sensitive files like `.env` or development-only configurations.
